# ros-rest-bridge
***Using the Ros-Rest-bridge***

* Building Docker image: docker build -t ros-rest-bridge .
* Running Docker image: docker run --network="host" -e "LOG_TO_CONSOLE=true" ros-rest-bridge



    